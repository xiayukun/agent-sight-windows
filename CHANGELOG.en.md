# Changelog

[中文](CHANGELOG.md) | English

## Unreleased

- Public branding is aligned around `AgentSight for Windows` while keeping `ai_control` / `ai-control` runtime compatibility.
- Release documentation now follows a Chinese-primary + English mirror structure.
- Tray GUI adds Chinese/English menu and tooltip localization, persisted in `%LOCALAPPDATA%\ai-control\tray-settings.json`.
- Tray icon changes to transparent-background colored `AI` letters instead of a square or circular color block.
- Release workflow supports `v*` tags and manual dispatch, runs tests, builds executables, creates SHA256 checksums, and uploads artifacts.
- Visual-memory docs frame `scale_down`, region, crop, `view_id`, and `time.near` as the attention toolbox.
